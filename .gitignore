# Окружение
.env
.venv/
venv/

# Локальное состояние дедупа (обработанные вакансии)
processed_*.json

# Локальная БД вакансий (+ WAL-спутники)
vacancies.db
vacancies.db-wal
vacancies.db-shm

# Состояние/логи управляющего бота
reader.pid
bot.lock
channels.lock
logs/
settings.json

# Сессия Telethon (содержит авторизацию — не коммитить!)
*.session
*.session-journal

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
